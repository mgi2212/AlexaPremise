namespace PremiseAlexaBridgeService
{
    //public static class InputExtensions
    //{
    //    #region Methods

    // public static void AddUnique<TList>(this IList<TList> self, IEnumerable<TList> items) {
    // foreach (var item in items) if (!self.Contains(item)) self.Add(item); }

    // public static double LimitToRange( this double value, double inclusiveMinimum, double
    // inclusiveMaximum) { if (value < inclusiveMinimum) { return inclusiveMinimum; } if (value >
    // inclusiveMaximum) { return inclusiveMaximum; } return value; }

    // public static int LimitToRange( this int value, int inclusiveMinimum, int inclusiveMaximum) {
    // if (value < inclusiveMinimum) { return inclusiveMinimum; } if (value > inclusiveMaximum) {
    // return inclusiveMaximum; } return value; }

    //    #endregion Methods
    //}

    //public class PreWarmCache : IProcessHostPreloadClient
    //{
    //    #region Methods

    // public void Preload(string[] parameters) { Task.Run(() => PremiseServer.WarmUpCache()); }

    //    #endregion Methods
    //}
}